# Policy overrides

- `default.json` defines global policy defaults.
- Optional per-repo overrides are loaded from `<repo>.json` files in this directory.
- Use neutral repository names and avoid keeping outdated repository-specific files here.
- Older filenames can be moved out of this folder when no longer used.
