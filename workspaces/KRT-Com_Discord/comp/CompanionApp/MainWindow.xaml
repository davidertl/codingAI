<Window x:Class="CompanionApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:util="clr-namespace:CompanionApp.Utilities"
        Title="das-KRT Companion | Alpha 0.0.9" Height="750" Width="950"
        MinHeight="650" MinWidth="850"
        WindowStartupLocation="CenterScreen"
        Icon="app-icon.ico"
        Background="#1E1E2E">
  <Window.Resources>
    <!-- Dark theme colors -->
    <SolidColorBrush x:Key="BackgroundDark" Color="#1E1E2E"/>
    <SolidColorBrush x:Key="BackgroundMedium" Color="#2D2D3D"/>
    <SolidColorBrush x:Key="BackgroundLight" Color="#3D3D4D"/>
    <SolidColorBrush x:Key="AccentBlue" Color="#4A9EFF"/>
    <SolidColorBrush x:Key="AccentGreen" Color="#4AFF9E"/>
    <SolidColorBrush x:Key="AccentRed" Color="#FF4A4A"/>
    <SolidColorBrush x:Key="AccentYellow" Color="#FFD84A"/>
    <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#A0A0B0"/>
    <SolidColorBrush x:Key="FreqDisplayBg" Color="#0A0A12"/>
    <SolidColorBrush x:Key="FreqDisplayText" Color="#00FF88"/>
    <SolidColorBrush x:Key="EmergencyBorder" Color="#FF3333"/>

    <!-- Button Style -->
    <Style x:Key="ModernButton" TargetType="Button">
      <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
      <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" 
                    CornerRadius="4" 
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <!-- Clear Button Style (Red X) -->
    <Style x:Key="ClearButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="4"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="2" Padding="{TemplateBinding Padding}">
              <TextBlock Text="✕" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#40FF4A4A"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <!-- TextBox Style -->
    <Style x:Key="ModernTextBox" TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
      <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="8,6"/>
      <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
    </Style>

    <!-- Frequency Input Style (smaller) -->
    <Style x:Key="FreqInputBox" TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
      <Setter Property="Foreground" Value="{StaticResource FreqDisplayText}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="4,2"/>
      <Setter Property="FontFamily" Value="Consolas"/>
      <Setter Property="FontSize" Value="16"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="TextAlignment" Value="Center"/>
      <Setter Property="MaxLength" Value="4"/>
      <Setter Property="Width" Value="60"/>
      <Setter Property="CaretBrush" Value="{StaticResource FreqDisplayText}"/>
    </Style>

    <!-- Hotkey TextBox Style -->
    <Style x:Key="HotkeyTextBox" TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
      <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="BorderBrush" Value="{StaticResource BackgroundMedium}"/>
      <Setter Property="Padding" Value="8,6"/>
      <Setter Property="IsReadOnly" Value="True"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="TextAlignment" Value="Center"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Style.Triggers>
        <Trigger Property="IsFocused" Value="True">
          <Setter Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
          <Setter Property="Background" Value="#252535"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <!-- Tab Style -->
    <Style TargetType="TabItem">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
      <Setter Property="Padding" Value="20,12"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Border x:Name="TabBorder" Background="{TemplateBinding Background}" 
                    Padding="{TemplateBinding Padding}" Margin="0,0,4,0">
              <ContentPresenter ContentSource="Header"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource BackgroundMedium}"/>
                <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource BackgroundLight}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Slider Style -->
    <Style TargetType="Slider">
      <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
      <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
    </Style>

    <!-- Boolean to Visibility Converter -->
    <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    <util:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
    <util:StringToVisibilityConverter x:Key="StringToVis"/>
    <util:BoolToHiddenVisibilityConverter x:Key="BoolToHidden"/>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header Status Bar -->
    <Border Grid.Row="0" Background="{StaticResource BackgroundMedium}" Padding="16,10">
      <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
          <TextBlock Text="das-KRT Companion" FontSize="16" FontWeight="Bold" 
                     Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
          <TextBlock Text="Alpha 0.0.9" FontSize="11" 
                     Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="10,2,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Ellipse Width="10" Height="10" Margin="0,0,6,0" VerticalAlignment="Center">
            <Ellipse.Style>
              <Style TargetType="Ellipse">
                <Setter Property="Fill" Value="{StaticResource AccentRed}"/>
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsVoiceConnected}" Value="True">
                    <Setter Property="Fill" Value="{StaticResource AccentGreen}"/>
                  </DataTrigger>
                  <DataTrigger Binding="{Binding IsVoiceReconnecting}" Value="True">
                    <Setter Property="Fill" Value="{StaticResource AccentYellow}"/>
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Ellipse.Style>
          </Ellipse>
          <TextBlock Text="{Binding VoiceConnectionIndicator}" Foreground="{StaticResource TextSecondary}" 
                     VerticalAlignment="Center" Margin="0,0,20,0"/>
          <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextSecondary}" 
                     VerticalAlignment="Center" MaxWidth="400" TextTrimming="CharacterEllipsis"/>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Main Tab Control -->
    <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Padding="0"
                SelectionChanged="TabControl_SelectionChanged">
      
      <!-- Radio Tab -->
      <TabItem Header="Radio">
        <Grid Margin="16">
          <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <!-- Radio Panels -->
          <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
              <!-- Emergency Radio Panel (special styling) -->
              <Border Background="#2A1515" CornerRadius="8" BorderThickness="2" BorderBrush="{StaticResource EmergencyBorder}"
                      Margin="0,0,0,16" Padding="16" Visibility="{Binding EmergencyRadioVisibility}">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>

                  <!-- Enable Checkbox -->
                  <CheckBox Grid.Column="0" IsChecked="{Binding EmergencyRadio.IsEnabled}" VerticalAlignment="Center" 
                            Margin="0,0,8,0" ToolTip="Enable emergency radio"/>

                  <!-- Status Arrows -->
                  <StackPanel Grid.Column="1" VerticalAlignment="Center" Width="30" Margin="0,0,12,0">
                    <TextBlock Text="▲" FontSize="16" Foreground="{Binding EmergencyRadio.StatusUpColor}" 
                               Visibility="{Binding EmergencyRadio.StatusArrowUp}" HorizontalAlignment="Center"/>
                    <TextBlock Text="▼" FontSize="16" Foreground="{Binding EmergencyRadio.StatusDownColor}" 
                               Visibility="{Binding EmergencyRadio.StatusArrowDown}" HorizontalAlignment="Center"/>
                  </StackPanel>

                  <!-- Label and Frequency -->
                  <StackPanel Grid.Column="2">
                    <TextBlock Text="EMERGENCY" FontWeight="Bold" FontSize="14" 
                               Foreground="{StaticResource EmergencyBorder}" Margin="0,0,0,6"/>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <!-- Frequency Display -->
                      <Border Grid.Column="0" Background="{StaticResource FreqDisplayBg}" CornerRadius="4" Padding="12,6">
                        <StackPanel>
                          <TextBlock Text="{Binding EmergencyRadio.FrequencyDisplay}" FontFamily="Consolas" FontSize="24" 
                                     Foreground="{StaticResource FreqDisplayText}"/>
                          <TextBlock Text="{Binding EmergencyRadio.ChannelName}" FontSize="10" FontStyle="Italic"
                                     Foreground="{StaticResource FreqDisplayText}" Opacity="0.7" Margin="0,2,0,0"
                                     Visibility="{Binding EmergencyRadio.HasChannelName, Converter={StaticResource BoolToHidden}}"/>
                        </StackPanel>
                      </Border>
                      <!-- Recent Callers (black display right of frequency) -->
                      <Border Grid.Column="1" Background="#111111" CornerRadius="4" Padding="8,6" Margin="8,0,0,0" MinWidth="140">
                        <StackPanel>
                          <TextBlock Text="Recent:" Foreground="{StaticResource EmergencyBorder}" FontSize="9" FontWeight="SemiBold" Margin="0,0,0,2"/>
                          <ItemsControl ItemsSource="{Binding EmergencyRadio.RecentTransmissions}" MaxHeight="50">
                            <ItemsControl.ItemTemplate>
                              <DataTemplate>
                                <TextBlock Text="{Binding Text}" Foreground="#FF6644" FontSize="10" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"/>
                              </DataTemplate>
                            </ItemsControl.ItemTemplate>
                          </ItemsControl>
                        </StackPanel>
                      </Border>
                    </Grid>
                  </StackPanel>

                  <!-- Listener Count -->
                  <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="20,0">
                    <TextBlock Text="Listeners" FontSize="10" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding EmergencyRadio.ListenerCount}" FontSize="18" FontWeight="Bold" 
                               Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center"/>
                  </StackPanel>

                  <!-- Volume -->
                  <StackPanel Grid.Column="4" VerticalAlignment="Center" Width="100" Margin="10,0">
                    <TextBlock Text="Volume" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                    <Slider Minimum="0" Maximum="100" Value="{Binding EmergencyRadio.Volume}"/>
                  </StackPanel>

                  <!-- Pan/Balance -->
                  <StackPanel Grid.Column="5" VerticalAlignment="Center" Width="100" Margin="10,0,0,0">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="Pan" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                      <TextBlock Grid.Column="1" Text="{Binding EmergencyRadio.BalanceText}" FontSize="10" 
                                 Foreground="{StaticResource TextSecondary}" TextAlignment="Right"/>
                    </Grid>
                    <Slider Minimum="0" Maximum="100" Value="{Binding EmergencyRadio.Balance}"/>
                  </StackPanel>
                </Grid>
              </Border>

              <!-- Regular Radio Panels Grid -->
              <ItemsControl ItemsSource="{Binding RadioPanels}">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <UniformGrid Columns="2"/>
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" 
                            Margin="6" Padding="12" Visibility="{Binding IsVisible}">
                      <Grid>
                        <Grid.RowDefinitions>
                          <RowDefinition Height="Auto"/>
                          <RowDefinition Height="Auto"/>
                          <RowDefinition Height="Auto"/>
                          <RowDefinition Height="Auto"/>
                          <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header Row -->
                        <Grid Grid.Row="0" Margin="0,0,0,8">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                          </Grid.ColumnDefinitions>

                          <!-- Status Arrows -->
                          <StackPanel Grid.Column="0" Orientation="Horizontal" Width="30" Margin="0,0,8,0">
                            <TextBlock Text="▲" FontSize="14" Foreground="{Binding StatusUpColor}" 
                                       Visibility="{Binding StatusArrowUp}"/>
                            <TextBlock Text="▼" FontSize="14" Foreground="{Binding StatusDownColor}" 
                                       Visibility="{Binding StatusArrowDown}"/>
                          </StackPanel>

                          <!-- Enable Checkbox -->
                          <CheckBox Grid.Column="1" IsChecked="{Binding IsEnabled}" VerticalAlignment="Center" Margin="0,0,8,0"
                                    ToolTip="Enable this radio"/>

                          <!-- Label Input -->
                          <TextBox Grid.Column="2" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}" 
                                   Style="{StaticResource ModernTextBox}" FontWeight="SemiBold" MaxWidth="150"/>

                          <!-- Listener Count -->
                          <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="10,0">
                            <TextBlock Text="👥" FontSize="12" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ListenerCount}" Foreground="{StaticResource TextSecondary}" 
                                       VerticalAlignment="Center" Margin="4,0,0,0"/>
                          </StackPanel>

                          <!-- Mute + Broadcast stacked -->
                          <StackPanel Grid.Column="4" VerticalAlignment="Center">
                            <CheckBox IsChecked="{Binding IsMuted}" Margin="0,0,0,2"
                                      ToolTip="Mute this radio" Content="🔇"/>
                            <CheckBox Content="📢" IsChecked="{Binding IncludedInBroadcast}" 
                                      ToolTip="Include in Talk To All broadcast"
                                      Visibility="{Binding ShowBroadcastCheckbox, Converter={StaticResource BoolToVis}}"/>
                          </StackPanel>
                        </Grid>

                        <!-- Frequency Display -->
                        <Border Grid.Row="1" Background="{StaticResource FreqDisplayBg}" CornerRadius="4" 
                                Padding="12,8" Margin="0,0,0,8">
                          <StackPanel>
                            <Grid>
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                              </Grid.ColumnDefinitions>
                              <TextBlock Grid.Column="0" Text="{Binding FrequencyDisplay}" 
                                         FontFamily="Consolas" FontSize="28" FontWeight="Bold"
                                         Foreground="{StaticResource FreqDisplayText}" VerticalAlignment="Center"/>
                              <TextBox Grid.Column="1" Text="{Binding FreqInput, UpdateSourceTrigger=PropertyChanged}" 
                                       Style="{StaticResource FreqInputBox}" VerticalAlignment="Center"/>
                            </Grid>
                            <!-- Channel name from Discord (shown when available, Hidden to preserve layout) -->
                            <TextBlock Text="{Binding ChannelName}" FontSize="11" FontStyle="Italic"
                                       Foreground="{StaticResource FreqDisplayText}" Opacity="0.7" Margin="0,2,0,0"
                                       Visibility="{Binding HasChannelName, Converter={StaticResource BoolToHidden}}"/>
                          </StackPanel>
                        </Border>

                        <!-- Volume and Balance Row -->
                        <Grid Grid.Row="2" Margin="0,0,0,8">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                          </Grid.ColumnDefinitions>

                          <!-- Volume -->
                          <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <Grid>
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                              </Grid.ColumnDefinitions>
                              <TextBlock Text="Vol" Foreground="{StaticResource TextSecondary}" FontSize="10" 
                                         VerticalAlignment="Center" Margin="0,0,6,0"/>
                              <Slider Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding Volume}" VerticalAlignment="Center"/>
                              <TextBlock Grid.Column="2" Text="{Binding Volume, StringFormat={}{0}%}" FontSize="10"
                                         Foreground="{StaticResource TextSecondary}" TextAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                          </StackPanel>

                          <!-- Balance -->
                          <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <Grid>
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                              </Grid.ColumnDefinitions>
                              <TextBlock Text="Pan" Foreground="{StaticResource TextSecondary}" FontSize="10" 
                                         VerticalAlignment="Center" Margin="0,0,6,0"/>
                              <Slider Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding Balance}" VerticalAlignment="Center"/>
                              <TextBlock Grid.Column="2" Text="{Binding BalanceText}" FontSize="10"
                                         Foreground="{StaticResource TextSecondary}" TextAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                          </StackPanel>
                        </Grid>

                        <!-- Hotkey Row -->
                        <Grid Grid.Row="3" Margin="0,0,0,8">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                          </Grid.ColumnDefinitions>
                          <TextBlock Text="PTT" Foreground="{StaticResource TextSecondary}" FontSize="10" 
                                     VerticalAlignment="Center" Margin="0,0,8,0"/>
                          <TextBox Grid.Column="1" Text="{Binding Hotkey, UpdateSourceTrigger=PropertyChanged}" 
                                   Style="{StaticResource HotkeyTextBox}" PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                   GotFocus="HotkeyTextBox_GotFocus" LostFocus="HotkeyTextBox_LostFocus" Tag="{Binding}"/>
                          <Button Grid.Column="2" Style="{StaticResource ClearButton}" Width="24" Height="24" Margin="4,0,0,0"
                                  Command="{Binding ClearHotkeyCommand}"/>
                        </Grid>

                        <!-- Recent Transmissions -->
                        <StackPanel Grid.Row="4">
                          <TextBlock Text="Recent:" Foreground="{StaticResource TextSecondary}" FontSize="9" Margin="0,0,0,2"/>
                          <ItemsControl ItemsSource="{Binding RecentTransmissions}" MaxHeight="40">
                            <ItemsControl.ItemTemplate>
                              <DataTemplate>
                                <TextBlock Text="{Binding Text}" Foreground="{StaticResource TextSecondary}" FontSize="9" 
                                           TextTrimming="CharacterEllipsis"/>
                              </DataTemplate>
                            </ItemsControl.ItemTemplate>
                          </ItemsControl>
                        </StackPanel>
                      </Grid>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </ScrollViewer>
        </Grid>
      </TabItem>

      <!-- Keybinds Tab -->
      <TabItem Header="Keybinds">
        <ScrollViewer Margin="20">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="20"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Column: Radio Hotkeys -->
            <StackPanel Grid.Column="0">
              <TextBlock Text="Click on a hotkey field and press your desired key combination" 
                         Foreground="{StaticResource TextSecondary}" Margin="0,0,0,15" FontSize="11" TextWrapping="Wrap"/>
              
              <TextBlock Text="Radio PTT Keys" Foreground="{StaticResource TextPrimary}" 
                         FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>

              <!-- Emergency Radio Keybind -->
              <Border Background="#2A1515" CornerRadius="6" BorderThickness="1" BorderBrush="{StaticResource EmergencyBorder}"
                      Margin="0,0,0,6" Padding="12" Visibility="{Binding EmergencyRadioVisibility}">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>
                  <TextBlock Grid.Column="0" Text="Emergency" Foreground="{StaticResource EmergencyBorder}" 
                             FontWeight="SemiBold" VerticalAlignment="Center"/>
                  <TextBox Grid.Column="1" Text="{Binding EmergencyRadio.Hotkey, UpdateSourceTrigger=PropertyChanged}" 
                           Style="{StaticResource HotkeyTextBox}" PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                           GotFocus="HotkeyTextBox_GotFocus" LostFocus="HotkeyTextBox_LostFocus"
                           Tag="{Binding EmergencyRadio}"/>
                  <Button Grid.Column="2" Style="{StaticResource ClearButton}" Width="28" Height="28" Margin="8,0,0,0"
                          Command="{Binding EmergencyRadio.ClearHotkeyCommand}"/>
                </Grid>
              </Border>
              
              <!-- Regular Radio Keybinds -->
              <ItemsControl ItemsSource="{Binding RadioPanels}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Background="{StaticResource BackgroundMedium}" CornerRadius="6" 
                            Margin="0,0,0,6" Padding="12" Visibility="{Binding IsVisible}">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="100"/>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Label}" 
                                   Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Hotkey, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource HotkeyTextBox}"
                                 PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                 GotFocus="HotkeyTextBox_GotFocus"
                                 LostFocus="HotkeyTextBox_LostFocus"
                                 Tag="{Binding}"/>
                        <Button Grid.Column="2" Style="{StaticResource ClearButton}" Width="28" Height="28" Margin="8,0,0,0"
                                Command="{Binding ClearHotkeyCommand}"/>
                      </Grid>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>

            <!-- Right Column: Global Hotkeys -->
            <StackPanel Grid.Column="2">
              <TextBlock Text=" " 
                         Foreground="{StaticResource TextSecondary}" Margin="0,0,0,15" FontSize="11"/>
              
              <TextBlock Text="Global Hotkeys" Foreground="{StaticResource TextPrimary}" 
                         FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
              
              <!-- Talk to All (Advanced Mode Only) -->
              <Border Background="{StaticResource BackgroundMedium}" CornerRadius="6" Padding="12" Margin="0,0,0,6"
                      Visibility="{Binding AdvancedMode, Converter={StaticResource BoolToVis}}">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>
                  <TextBlock Text="Talk To All" Foreground="{StaticResource AccentBlue}" VerticalAlignment="Center"
                             ToolTip="Broadcast to all radios with the broadcast checkbox enabled"/>
                  <TextBox x:Name="TalkToAllHotkeyBox" Grid.Column="1" Text="{Binding TalkToAllHotkey}" Style="{StaticResource HotkeyTextBox}"
                           PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" GotFocus="HotkeyTextBox_GotFocus"
                           LostFocus="HotkeyTextBox_LostFocus"/>
                  <Button Grid.Column="2" Style="{StaticResource ClearButton}" Width="28" Height="28" Margin="8,0,0,0"
                          Click="ClearTalkToAllHotkey_Click"/>
                </Grid>
              </Border>

              <!-- PTM All Radio -->
              <Border Background="{StaticResource BackgroundMedium}" CornerRadius="6" Padding="12" Margin="0,0,0,6">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>
                  <TextBlock Text="PTM All Radio" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"
                             ToolTip="Push-to-Mute: All radios will be muted while this key is pressed"/>
                  <TextBox x:Name="PttMuteAllHotkeyBox" Grid.Column="1" Text="{Binding PttMuteAllHotkey}" Style="{StaticResource HotkeyTextBox}"
                           PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" GotFocus="HotkeyTextBox_GotFocus"
                           LostFocus="HotkeyTextBox_LostFocus"/>
                  <Button Grid.Column="2" Style="{StaticResource ClearButton}" Width="28" Height="28" Margin="8,0,0,0"
                          Click="ClearPttMuteAllHotkey_Click"/>
                </Grid>
              </Border>

              <!-- TTM All Radio -->
              <Border Background="{StaticResource BackgroundMedium}" CornerRadius="6" Padding="12" Margin="0,0,0,6">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>
                  <TextBlock Text="TTM All Radio" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"
                             ToolTip="Toggle-to-Mute: All radios will be muted until you press this key again"/>
                  <TextBox x:Name="ToggleMuteAllHotkeyBox" Grid.Column="1" Text="{Binding ToggleMuteAllHotkey}" Style="{StaticResource HotkeyTextBox}"
                           PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" GotFocus="HotkeyTextBox_GotFocus"
                           LostFocus="HotkeyTextBox_LostFocus"/>
                  <Button Grid.Column="2" Style="{StaticResource ClearButton}" Width="28" Height="28" Margin="8,0,0,0"
                          Click="ClearToggleMuteAllHotkey_Click"/>
                </Grid>
              </Border>

              <!-- Mute Status Indicator -->
              <Border Background="{StaticResource BackgroundMedium}" CornerRadius="6" Padding="12" Margin="0,20,0,0">
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="All Radios Muted:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                  <Ellipse Width="12" Height="12" Margin="10,0,0,0" VerticalAlignment="Center">
                    <Ellipse.Style>
                      <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="{StaticResource BackgroundLight}"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding AllRadiosMuted}" Value="True">
                            <Setter Property="Fill" Value="{StaticResource AccentRed}"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </Ellipse.Style>
                  </Ellipse>
                </StackPanel>
              </Border>
            </StackPanel>
          </Grid>
        </ScrollViewer>
      </TabItem>

      <!-- App Settings Tab -->
      <TabItem Header="App Settings">
        <ScrollViewer Margin="20">
          <StackPanel MaxWidth="900">
            <TextBlock Text="Application Settings" Foreground="{StaticResource TextPrimary}" 
                       FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

            <!-- General + Autostart cards side by side -->
            <Grid Margin="0,0,0,16">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>

              <!-- General Card -->
              <Border Grid.Column="0" Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="20">
                <StackPanel>
                  <TextBlock Text="General" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold" Margin="0,0,0,15"/>
                  
                  <CheckBox Content="Play sound on PTT start/end" Foreground="{StaticResource TextSecondary}" 
                            IsChecked="{Binding PlayPttBeep}" Margin="0,0,0,6"/>
                  <StackPanel Margin="18,0,0,10">
                    <StackPanel.Style>
                      <Style TargetType="StackPanel">
                        <Setter Property="Opacity" Value="1"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding PlayPttBeep}" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </StackPanel.Style>
                    <CheckBox Content="Enable sound on receiving" Foreground="{StaticResource TextSecondary}"
                              IsChecked="{Binding PlaySoundOnReceive}" IsEnabled="{Binding PlayPttBeep}" Margin="0,0,0,4"/>
                    <CheckBox Content="Enable sound on transmitting" Foreground="{StaticResource TextSecondary}"
                              IsChecked="{Binding PlaySoundOnTransmit}" IsEnabled="{Binding PlayPttBeep}" Margin="0,0,0,4"/>
                    <CheckBox Content="Enable sound on beginning" Foreground="{StaticResource TextSecondary}"
                              IsChecked="{Binding PlaySoundOnBegin}" IsEnabled="{Binding PlayPttBeep}" Margin="0,0,0,4"/>
                    <CheckBox Content="Enable sound on end" Foreground="{StaticResource TextSecondary}"
                              IsChecked="{Binding PlaySoundOnEnd}" IsEnabled="{Binding PlayPttBeep}" Margin="0,0,0,4"/>
                  </StackPanel>

                  <CheckBox Content="Enable Emergency Radio" Foreground="{StaticResource TextSecondary}" 
                            IsChecked="{Binding EnableEmergencyRadio}" Margin="0,0,0,6"/>
                  <StackPanel Margin="18,0,0,0">
                    <CheckBox Content="Turn on Emergency radio on startup" Foreground="{StaticResource TextSecondary}" 
                              IsChecked="{Binding TurnOnEmergencyOnStartup}" Margin="0,0,0,4"
                              IsEnabled="{Binding EnableEmergencyRadio}"/>
                  </StackPanel>
                </StackPanel>
              </Border>

              <!-- Autostart + Debug Card -->
              <Border Grid.Column="2" Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="20">
                <StackPanel>
                  <TextBlock Text="Autostart" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold" Margin="0,0,0,15"/>

                  <CheckBox Content="Launch on Windows startup" Foreground="{StaticResource TextSecondary}" 
                            IsChecked="{Binding LaunchOnStartup}" Margin="0,0,0,6"/>
                  <StackPanel Margin="18,0,0,10">
                    <CheckBox Content="Launch minimized to tray" Foreground="{StaticResource TextSecondary}" 
                              IsChecked="{Binding StartMinimized}" Margin="0,0,0,4"
                              IsEnabled="{Binding LaunchOnStartup}"/>
                  </StackPanel>
                  <CheckBox Content="Auto-connect on launch" Foreground="{StaticResource TextSecondary}" 
                            IsChecked="{Binding AutoConnect}" Margin="0,0,0,10"/>
                  <CheckBox Content="Recover active state from radios" Foreground="{StaticResource TextSecondary}" 
                            IsChecked="{Binding SaveRadioActiveState}" Margin="0,0,0,10"
                            ToolTip="When enabled, radios remember their on/off state between sessions. Otherwise radios start inactive."/>

                  <TextBlock Text="Debug" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold" Margin="0,15,0,10"/>
                  <CheckBox Content="Enable debug logging" Foreground="{StaticResource TextSecondary}"
                            IsChecked="{Binding DebugLoggingEnabled}" Margin="0,0,0,6"/>
                  <TextBlock Text="Log file:" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                  <TextBlock Text="{Binding DebugLogFilePath}" Foreground="{StaticResource TextSecondary}" 
                             FontSize="11" TextWrapping="Wrap" Margin="0,0,0,0"/>
                </StackPanel>
              </Border>
            </Grid>

            <!-- Overlay Card -->
            <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="20" Margin="0,0,0,16">
              <StackPanel>
                <TextBlock Text="In-Game Overlay" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBlock Text="Shows who last transmitted on which frequency as a topmost overlay." 
                           Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,10"/>

                <CheckBox Content="Enable overlay" Foreground="{StaticResource TextSecondary}"
                          IsChecked="{Binding OverlayEnabled}" Margin="0,0,0,8"/>

                <StackPanel IsEnabled="{Binding OverlayEnabled}">
                  <StackPanel.Style>
                    <Style TargetType="StackPanel">
                      <Setter Property="Opacity" Value="1"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding OverlayEnabled}" Value="False">
                          <Setter Property="Opacity" Value="0.45"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </StackPanel.Style>

                  <CheckBox Content="Show rank" Foreground="{StaticResource TextSecondary}"
                            IsChecked="{Binding OverlayShowRank}" Margin="0,0,0,4"
                            ToolTip="Display the user's rank next to their name (requires rank sync)"/>
                  <CheckBox Content="Show radio keybind" Foreground="{StaticResource TextSecondary}"
                            IsChecked="{Binding OverlayShowRadioKeybind}" Margin="0,0,0,10"
                            ToolTip="Display the hotkey assigned to the radio next to the frequency"/>

                  <TextBlock Text="Background opacity" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,4"/>
                  <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="45"/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0" Minimum="10" Maximum="100" Value="{Binding OverlayOpacity}" 
                            VerticalAlignment="Center" TickFrequency="10" IsSnapToTickEnabled="False"
                            ToolTip="How opaque the overlay appears: 10% = nearly transparent, 100% = fully opaque"/>
                    <TextBlock Grid.Column="1" Text="{Binding OverlayOpacityText}" Foreground="{StaticResource TextSecondary}" 
                               VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="12"/>
                  </Grid>

                  <CheckBox Content="Auto-hide inactive radios" Foreground="{StaticResource TextSecondary}"
                            IsChecked="{Binding OverlayAutoHideEnabled}" Margin="0,0,0,4"
                            ToolTip="Remove a radio from the overlay when no one has transmitted for the configured duration"/>
                  <TextBlock Text="Hide after (seconds)" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,4">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock">
                        <Setter Property="Opacity" Value="1"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding OverlayAutoHideEnabled}" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>
                  <Grid Margin="0,0,0,10" IsEnabled="{Binding OverlayAutoHideEnabled}">
                    <Grid.Style>
                      <Style TargetType="Grid">
                        <Setter Property="Opacity" Value="1"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding OverlayAutoHideEnabled}" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </Grid.Style>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="45"/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0" Minimum="5" Maximum="300" Value="{Binding OverlayAutoHideSeconds}"
                            VerticalAlignment="Center" TickFrequency="5" IsSnapToTickEnabled="True"
                            ToolTip="Seconds of inactivity before a radio is hidden from the overlay"/>
                    <TextBlock Grid.Column="1" Text="{Binding OverlayAutoHideText}" Foreground="{StaticResource TextSecondary}"
                               VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="12"/>
                  </Grid>

                  <TextBlock Text="Position (screen pixels from top-left)" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,4"/>
                  <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto"/>
                      <ColumnDefinition Width="80"/>
                      <ColumnDefinition Width="20"/>
                      <ColumnDefinition Width="Auto"/>
                      <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="X:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox Grid.Column="1" Text="{Binding OverlayPositionX, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}"/>
                    <TextBlock Grid.Column="3" Text="Y:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox Grid.Column="4" Text="{Binding OverlayPositionY, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}"/>
                  </Grid>
                </StackPanel>
              </StackPanel>
            </Border>

            <!-- Voice Ducking Card -->
            <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="20" Margin="0,0,0,16">
              <StackPanel>
                <TextBlock Text="Voice Ducking" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBlock Text="Reduce audio volume during voice activity." 
                           Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,10"/>

                <CheckBox Content="Enable ducking" Foreground="{StaticResource TextSecondary}"
                          IsChecked="{Binding DuckingEnabled}" Margin="0,0,0,8"/>

                <!-- When to duck -->
                <StackPanel Margin="18,0,0,12" IsEnabled="{Binding DuckingEnabled}">
                  <StackPanel.Style>
                    <Style TargetType="StackPanel">
                      <Setter Property="Opacity" Value="1"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding DuckingEnabled}" Value="False">
                          <Setter Property="Opacity" Value="0.45"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </StackPanel.Style>
                  <CheckBox Content="Duck when sending (PTT held)" Foreground="{StaticResource TextSecondary}"
                            IsChecked="{Binding DuckOnSend}" Margin="0,0,0,4"
                            ToolTip="Reduce audio while you are transmitting"/>
                  <CheckBox Content="Duck when receiving" Foreground="{StaticResource TextSecondary}"
                            IsChecked="{Binding DuckOnReceive}" Margin="0,0,0,0"
                            ToolTip="Reduce audio while someone else is transmitting on your radio"/>
                </StackPanel>

                <!-- Controls below are enabled only when ducking is on -->
                <StackPanel IsEnabled="{Binding DuckingEnabled}">
                  <StackPanel.Style>
                    <Style TargetType="StackPanel">
                      <Setter Property="Opacity" Value="1"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding DuckingEnabled}" Value="False">
                          <Setter Property="Opacity" Value="0.45"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </StackPanel.Style>

                  <TextBlock Text="Volume during ducking" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,4"/>
                  <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="45"/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0" Minimum="0" Maximum="100" Value="{Binding DuckingLevel}" 
                            VerticalAlignment="Center" TickFrequency="25" IsSnapToTickEnabled="False"
                            ToolTip="How loud ducked audio stays: 0% = fully muted, 100% = no change"/>
                    <TextBlock Grid.Column="1" Text="{Binding DuckingLevelText}" Foreground="{StaticResource TextSecondary}" 
                               VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="12"/>
                  </Grid>

                  <TextBlock Text="Ducking Target" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,5"/>
                  <ComboBox SelectedIndex="{Binding DuckingMode}" Width="280" HorizontalAlignment="Left" Margin="0,0,0,10">
                    <ComboBoxItem Content="Radio audio only"/>
                    <ComboBoxItem Content="Selected apps"/>
                    <ComboBoxItem Content="All audio except KRT-Com"/>
                  </ComboBox>

                  <!-- Ducked process selector (visible when mode = "Selected apps") -->
                  <StackPanel Margin="0,0,0,4">
                    <StackPanel.Style>
                      <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding ShowDuckedProcessSelector}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </StackPanel.Style>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                      <TextBlock Text="Select apps to duck:" Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center"/>
                      <Button Content="Refresh" Style="{StaticResource ModernButton}" Click="RefreshAudioSessions_Click"
                              Margin="10,0,0,0" Padding="8,3" FontSize="11"/>
                    </StackPanel>
                    <Border Background="#1A1A2A" CornerRadius="4" Padding="8" MaxHeight="150">
                      <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding AvailableAudioSessions}">
                          <ItemsControl.ItemTemplate>
                            <DataTemplate>
                              <CheckBox Content="{Binding ProcessName}" IsChecked="{Binding IsSelected}"
                                        Foreground="{StaticResource TextSecondary}" Margin="0,2"
                                        Click="DuckedProcessCheckBox_Click"/>
                            </DataTemplate>
                          </ItemsControl.ItemTemplate>
                        </ItemsControl>
                      </ScrollViewer>
                    </Border>
                  </StackPanel>

                  <TextBlock Text="'Radio audio only' ducks other radio channels during voice activity. Select 'Selected apps' or 'All audio except KRT-Com' to also duck external applications." 
                             Foreground="{StaticResource TextSecondary}" FontSize="10" FontStyle="Italic" 
                             TextWrapping="Wrap" Margin="0,6,0,0"/>

                </StackPanel>
              </StackPanel>
            </Border>

            <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="20" Margin="0,0,0,16">
              <StackPanel>
                <TextBlock Text="Audio" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold" Margin="0,0,0,15"/>
                
                <TextBlock Text="Input Device (Microphone)" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding AudioInputDevices}" SelectedValue="{Binding SelectedAudioInputDevice}" 
                          Width="350" HorizontalAlignment="Left" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,15">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="45"/>
                  </Grid.ColumnDefinitions>
                  <Slider Grid.Column="0" Minimum="0" Maximum="125" Value="{Binding InputVolume}" 
                          VerticalAlignment="Center" ToolTip="Input volume (0-125%)"/>
                  <TextBlock Grid.Column="1" Text="{Binding InputVolumeText}" Foreground="{StaticResource TextSecondary}" 
                             VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="12"/>
                </Grid>

                <TextBlock Text="Output Device (Speakers)" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding AudioOutputDevices}" SelectedValue="{Binding SelectedAudioOutputDevice}" 
                          Width="350" HorizontalAlignment="Left" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,15">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="45"/>
                  </Grid.ColumnDefinitions>
                  <Slider Grid.Column="0" Minimum="0" Maximum="125" Value="{Binding OutputVolume}" 
                          VerticalAlignment="Center" ToolTip="Output volume (0-125%)"/>
                  <TextBlock Grid.Column="1" Text="{Binding OutputVolumeText}" Foreground="{StaticResource TextSecondary}" 
                             VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="12"/>
                </Grid>

                <TextBlock Text="Sample Rate" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                <ComboBox Width="150" HorizontalAlignment="Left">
                  <ComboBoxItem Content="48000 Hz" IsSelected="True"/>
                  <ComboBoxItem Content="44100 Hz"/>
                </ComboBox>
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <!-- Server Settings Tab -->
      <TabItem Header="Server">
        <ScrollViewer Margin="20">
          <StackPanel MaxWidth="500">
            <TextBlock Text="Server Connection" Foreground="{StaticResource TextPrimary}" 
                       FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

            <!-- Voice Server -->
            <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="20" Margin="0,0,0,16">
              <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                  <TextBlock Text="Voice Server" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold"/>
                  <Ellipse Width="10" Height="10" Margin="15,0,6,0" VerticalAlignment="Center">
                    <Ellipse.Style>
                      <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="{StaticResource AccentRed}"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding IsVoiceConnected}" Value="True">
                            <Setter Property="Fill" Value="{StaticResource AccentGreen}"/>
                          </DataTrigger>
                          <DataTrigger Binding="{Binding IsVoiceReconnecting}" Value="True">
                            <Setter Property="Fill" Value="{StaticResource AccentYellow}"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </Ellipse.Style>
                  </Ellipse>
                  <TextBlock Text="{Binding VoiceConnectionIndicator}" Foreground="{StaticResource TextSecondary}" 
                             VerticalAlignment="Center"/>
                  <Button Content="Disconnect" Style="{StaticResource ModernButton}" 
                          Click="VoiceConnect_Click" Margin="12,0,0,0" Padding="10,4" FontSize="11"
                          Visibility="{Binding IsVoiceConnected, Converter={StaticResource BoolToVis}}"
                          ToolTip="Disconnect voice connection"/>
                </StackPanel>
                
                <!-- Host + Port + Verify Button Row -->
                <Grid Margin="0,0,0,10">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>
                  <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Host" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding VoiceHost, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}"/>
                  </StackPanel>
                  <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Port" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding VoicePort, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}"/>
                  </StackPanel>
                  <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                    <Button Content="Verify" Style="{StaticResource ModernButton}" 
                            Click="Verify_Click" Width="80" ToolTip="Fetch server status and privacy policy"/>
                  </StackPanel>
                </Grid>

                <!-- Verify Status Text -->
                <TextBlock Text="{Binding VerifyStatusText}" Foreground="{StaticResource TextSecondary}" 
                           FontSize="11" Margin="0,0,0,10" TextWrapping="Wrap"/>

                <!-- Server Status Info (shown after verification) -->
                <Border Background="#252535" CornerRadius="6" Padding="15" Margin="0,0,0,10"
                        Visibility="{Binding IsServerVerified, Converter={StaticResource BoolToVis}}">
                  <StackPanel>
                    <TextBlock Text="Server Status" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    
                    <Grid Margin="0,0,0,6">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="Version:" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                      <TextBlock Grid.Column="1" Text="{Binding ServerVersion}" Foreground="{StaticResource TextPrimary}" FontSize="11"/>
                    </Grid>

                    <!-- Version mismatch warning -->
                    <Border Background="#3A3020" CornerRadius="4" Padding="8" Margin="0,4,0,6">
                      <Border.Style>
                        <Style TargetType="Border">
                          <Setter Property="Visibility" Value="Collapsed"/>
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding IsVersionMismatch}" Value="True">
                              <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </Border.Style>
                      <TextBlock Text="{Binding VersionMismatchText}"
                                 Foreground="{StaticResource AccentYellow}" FontSize="11" TextWrapping="Wrap"/>
                    </Border>

                    <Grid Margin="0,0,0,6">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="DSGVO Mode:" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                      <TextBlock Grid.Column="1" Text="{Binding DsgvoStatusText}" Foreground="{Binding DsgvoStatusColor}" FontSize="11" FontWeight="SemiBold"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="Debug Mode:" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                      <TextBlock Grid.Column="1" Text="{Binding DebugModeStatusText}" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                    </Grid>

                    <Grid Margin="0,0,0,4">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="Retention:" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                      <TextBlock Grid.Column="1" Text="{Binding ServerRetentionDays, StringFormat={}{0} days}" 
                                 Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                    </Grid>

                    <!-- DSGVO disabled warning -->
                    <Border Background="#3A2020" CornerRadius="4" Padding="8" Margin="0,8,0,0">
                      <Border.Style>
                        <Style TargetType="Border">
                          <Setter Property="Visibility" Value="Collapsed"/>
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding ServerDsgvoEnabled}" Value="False">
                              <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </Border.Style>
                      <TextBlock Text="Warning: DSGVO compliance mode is DISABLED on this server. Your data may not be automatically deleted."
                                 Foreground="{StaticResource AccentRed}" FontSize="11" TextWrapping="Wrap"/>
                    </Border>
                  </StackPanel>
                </Border>

                <!-- Privacy Policy Section (shown after verification) -->
                <Border Background="#252535" CornerRadius="6" Padding="15" Margin="0,0,0,10"
                        Visibility="{Binding IsServerVerified, Converter={StaticResource BoolToVis}}">
                  <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                      <TextBlock Text="Privacy Policy" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold"/>
                      <TextBlock Text="{Binding ServerPolicyVersion, StringFormat=' (v{0})'}" 
                                 Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Border Background="#1A1A2A" CornerRadius="4" Padding="10" MaxHeight="200">
                      <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding PrivacyPolicyText}" Foreground="{StaticResource TextSecondary}" 
                                   FontSize="11" TextWrapping="Wrap"/>
                      </ScrollViewer>
                    </Border>

                    <!-- Accept / Already Accepted -->
                    <StackPanel Margin="0,10,0,0">
                      <StackPanel Orientation="Horizontal" 
                                  Visibility="{Binding PolicyAccepted, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="✓ Privacy policy accepted" Foreground="{StaticResource AccentGreen}" FontSize="12"/>
                      </StackPanel>
                      <Button Content="I have read and accept the Privacy Policy" Style="{StaticResource ModernButton}"
                              Click="AcceptPolicy_Click"
                              Visibility="{Binding PolicyNeedsAcceptance, Converter={StaticResource BoolToVis}}"
                              HorizontalAlignment="Left"/>
                    </StackPanel>
                  </StackPanel>
                </Border>

                <!-- Discord Login (shown after server verification) -->
                <Border Background="#252535" CornerRadius="6" Padding="15" Margin="0,0,0,15"
                        Visibility="{Binding ServerVerifiedVisibility}">
                  <StackPanel>
                    <TextBlock Text="Authentication" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    
                    <!-- Logged-in display -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8"
                                Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVis}}">
                      <TextBlock Text="✓ Logged in as " Foreground="{StaticResource AccentGreen}" FontSize="12"/>
                      <TextBlock Text="{Binding LoggedInDisplayName}" Foreground="{StaticResource AccentGreen}" FontSize="12" FontWeight="SemiBold"/>
                    </StackPanel>

                    <!-- Hint when button is disabled (hidden when logged in) -->
                    <TextBlock Text="{Binding DiscordLoginHint}" Foreground="#FF8866" FontSize="11" Margin="0,0,0,8"
                               TextWrapping="Wrap"
                               Visibility="{Binding DiscordLoginHint, Converter={StaticResource StringToVis}}"/>

                    <!-- OAuth login status -->
                    <TextBlock Text="{Binding OAuthLoginStatus}" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,8"
                               TextWrapping="Wrap"
                               Visibility="{Binding OAuthLoginStatus, Converter={StaticResource StringToVis}}"/>

                    <!-- Login with Discord button (hidden when already logged in) -->
                    <Button Content="Login with Discord" Style="{StaticResource ModernButton}"
                            Click="LoginWithDiscord_Click"
                            IsEnabled="{Binding CanLoginWithDiscord}"
                            Visibility="{Binding IsLoggedIn, Converter={StaticResource InverseBoolToVis}}"
                            HorizontalAlignment="Left" Margin="0,0,0,8"/>

                    <!-- Voice connect/disconnect -->
                    <Button Content="{Binding VoiceConnectButtonText}" Style="{StaticResource ModernButton}" 
                            Click="VoiceConnect_Click" HorizontalAlignment="Left"
                            Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVis}}"
                            IsEnabled="{Binding IsLoggedIn}"/>
                  </StackPanel>
                </Border>
              </StackPanel>
            </Border>

            <!-- Debug Settings (only visible when server debug mode is active) -->
            <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="20"
                    Visibility="{Binding ServerDebugMode, Converter={StaticResource BoolToVis}}">
              <StackPanel>
                <TextBlock Text="Debug Settings" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <TextBlock Text="Admin Token" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding AdminToken, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTextBox}"/>
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>
    </TabControl>

    <!-- Footer -->
    <Border Grid.Row="2" Background="{StaticResource BackgroundMedium}" Padding="16,10">
      <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
          <Button x:Name="SaveButton" Content="Save" Click="Save_Click" Margin="0,0,10,0"
                  Foreground="White" BorderThickness="0" Padding="16,8" Cursor="Hand">
            <Button.Style>
              <Style TargetType="Button">
                <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
                <Setter Property="Template">
                  <Setter.Value>
                    <ControlTemplate TargetType="Button">
                      <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                      </Border>
                    </ControlTemplate>
                  </Setter.Value>
                </Setter>
                <Style.Triggers>
                  <DataTrigger Binding="{Binding HasUnsavedChanges}" Value="True">
                    <Setter Property="Background" Value="#00AA55"/>
                  </DataTrigger>
                  <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
                  </Trigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
          </Button>
          <Button Content="Reload" Style="{StaticResource ModernButton}" Click="Reload_Click" Margin="0,0,20,0"/>
          <CheckBox Content="Advanced Mode" Foreground="{StaticResource TextSecondary}" 
                    IsChecked="{Binding AdvancedMode}" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <TextBlock Text="Streaming:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,8,0"/>
          <Ellipse x:Name="StreamingIndicatorEllipse" Width="10" Height="10" Margin="0,0,20,0" VerticalAlignment="Center"
                   Fill="{Binding StreamingIndicatorColor}">
            <Ellipse.Style>
              <Style TargetType="Ellipse">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsStreaming}" Value="False">
                    <Setter Property="Fill" Value="{StaticResource BackgroundLight}"/>
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Ellipse.Style>
          </Ellipse>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Window>
